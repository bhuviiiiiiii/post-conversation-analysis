# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-change-this-in-production

# Database (leave as-is for SQLite development)
# For PostgreSQL: postgresql://user:password@localhost:5432/post_analysis

# Celery Broker (memory:// for dev, redis://localhost:6379/0 for production)
CELERY_BROKER_URL=memory://
CELERY_RESULT_BACKEND=cache+memory://

# Email (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True
